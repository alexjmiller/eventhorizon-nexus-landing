# eventhorizon.nexus — landing page

Static landing page for [eventhorizon.nexus](https://eventhorizon.nexus).

A single self-contained `index.html` — no dependencies, no build step. The animation is a raw WebGL2 fragment shader.

> **Note:** this is a public repository for the landing page only. It is not the product.
